import { SpellcheckerDirective } from './spellchecker.directive';

describe('SpellcheckerDirective', () => {
    it('should create an instance', () => {
        const directive = new SpellcheckerDirective(null);
        expect(directive).toBeTruthy();
    });
});
